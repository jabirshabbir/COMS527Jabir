Implemnetation is done using standard C libraries on visual studio community 
However pthread library requires on to installed 

The documentation comments are for CPU implemnetations.
The code is same for GPU except openmp is used in ops.c